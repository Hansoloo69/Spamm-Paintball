local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local backpack = player:WaitForChild("Backpack")

-- GUI erstellen
local screenGui = Instance.new("ScreenGui")
screenGui.Parent = player:WaitForChild("PlayerGui")

local toggleButton = Instance.new("TextButton")
toggleButton.Size = UDim2.new(0, 200, 0, 50)
toggleButton.Position = UDim2.new(0.5, -100, 0.9, -25)
toggleButton.Text = "Auto-Use: OFF"
toggleButton.Parent = screenGui

local autoUseEnabled = false
local itemName = "Paintball-Pistole"

toggleButton.MouseButton1Click:Connect(function()
    autoUseEnabled = not autoUseEnabled
    toggleButton.Text = autoUseEnabled and "Auto-Use: ON" or "Auto-Use: OFF"

    if autoUseEnabled then
        task.spawn(function()
            while autoUseEnabled do
                -- Tool suchen
                local tool = character:FindFirstChild(itemName) or backpack:FindFirstChild(itemName)
                
                if tool and tool:IsA("Tool") then
                    -- Tool ausrüsten, falls im Backpack
                    if tool.Parent ~= character then
                        tool.Parent = character
                        -- Warten, bis das Tool wirklich ausgerüstet ist
                        repeat task.wait() until tool.Parent == character
                    end

                    -- Tool aktivieren / benutzen
                    pcall(function()
                        tool:Activate()
                    end)
                end

                task.wait(0.001) -- kurze Pause zwischen Aktivierungen, damit es nicht zu schnell schießt
            end
        end)
    end
end)

